# CodePick · L3 应用与分发 — 开发文档（交给 Codex）

> **用法**：把本文件放进 L3 项目根目录，作为 Codex 的唯一输入。先读完全文，再按「提示词」开工。
> 自包含：提示词 + 产品 + 架构 + DDL + 详尽开发Plan(验收/自测) + 实施手册 + 共享契约。

---

## 0. 给 Codex 的提示词（从这里开始）

**角色**：资深全栈工程师（Next.js + Python/FastAPI），独立实现 CodePick 平台的 **L3「应用与分发」**（Reader Web + Public API/MCP + 每日早报 + 计费）。

**背景**：分层数据平台 **L0→L1→L2→L3**，4 层并行开发、最后联调。你只负责 L3。开发期用 **stub 假装 L2** + 支付沙箱；联调时换真实 L2 与真实支付。必须严格遵守第 8 节共享契约。

**任务**：按第 4 节《开发Plan》EPIC E0→E9、Feature 顺序实现，逐个用「验收」+「自测」收口；按第 5 节落地脚手架/接口契约/配置。

**硬约束**：
1. 独立开发：读内容/评分/翻译只经 `ContentReadProvider`，注入 Stub+fixtures；推荐/伴读用 stub；支付用沙箱。**不依赖真 L2、不接真实支付账户。**
2. **信任边界隔离**：内部 `/api`(JWT) 与对外 `/v1`(API Key+限流+配额) 分服务/前缀/过滤器；`/v1` 不泄漏推荐决策与隐私上下文。
3. **门禁集中**：Free/Pro 用单点 `require_plan`，禁止散落。
4. 表归属：只拥有 users/subscriptions/interests/follows/reading_events/briefs/bookmarks/api_keys/usage；只读 L0–L2。
5. 收款 **USD**（Paddle 作 MoR）；$8/月·$79/年(早鸟 $4.9)。北极星=深度阅读闭环率，不优化时长/DAU。
6. 栈：Next.js(App Router,TS,Tailwind,i18n)+FastAPI+SQLAlchemy/Alembic+PostgreSQL+Arq+Resend/SES。

**交付标准 (DoD)**：每 Feature 过自测；`make l3-smoke` 输出 `L3 PIPELINE: PASS`；前端 Playwright E2E + 后端 pytest(webhook 验签/配额/门禁) CI 全绿；满足第 7 节 DoD。产出：前后端代码+测试+迁移+README。

**工作方式**：每 Sprint 提交一次。契约歧义先提问，不擅改共享契约或绕过门禁/信任边界。

---

## 1. 产品定义

L3 是面向终端的产品集，消费 L2 的 COMPLETED 判断，做**体验、分发、商业化**。同一份判断，两种消费：人在 Reader 读，Agent 通过 API/MCP 调。

- **产品矩阵**：Reader Web、Daily Brief/Email、Public API /v1、MCP Server、Newsletter
- **Reader 功能**：公共精选(Free) / 详情双语(Free) / AI 伴读(限量→Pro) / 我的早报+推荐(Pro) / 关注+私有源(Pro) / 阅读库回顾(Pro)
- **客户**：人(开发者) + 机器(Agent)
- **边界**：不重算判断(L2)、不做内容理解/抓取(L0/L1)、不把未 COMPLETED 内容对外

---

## 2. 架构设计

```
浏览器 ──HTTP/SSE──▶ reader-web(Next.js SSR/i18n) ──/api/*──▶ reader-api(FastAPI,JWT)──读 L0–L2 产物 + 调 L2 推荐/伴读
Agent ──X-API-KEY──▶ public-api(/v1, Key+限流+配额)         Cron──▶ briefs-worker(早报+邮件)
Claude/Cursor ─MCP─▶ mcp-server ──调内部 /v1                支付 Webhook◀── Paddle/Stripe
共享存储: PostgreSQL+pgvector · Redis · 对象存储
```
**信任边界隔离**：内部 /api(JWT) 与对外 /v1(API Key) 分服务/前缀/过滤器；/v1 只暴露稳定原语，不透出推荐决策与隐私。**门禁集中**于 require_plan。

---

## 3. 数据库 DDL（L3 拥有的表）

```sql
CREATE TABLE users(id BIGSERIAL PRIMARY KEY, email TEXT UNIQUE, nickname TEXT, locale TEXT, plan TEXT DEFAULT 'free', created_at TIMESTAMPTZ DEFAULT now());
CREATE TABLE subscriptions(user_id BIGINT, plan TEXT, status TEXT, period_end TIMESTAMPTZ, provider TEXT, provider_ref TEXT);
CREATE TABLE user_interests(user_id BIGINT, tag_code TEXT, weight REAL, PRIMARY KEY(user_id, tag_code));
CREATE TABLE user_follows(user_id BIGINT, target_type TEXT, target_id TEXT);
CREATE TABLE reading_events(id BIGSERIAL PRIMARY KEY, user_id BIGINT, content_id BIGINT, type TEXT, ts TIMESTAMPTZ DEFAULT now());
CREATE TABLE briefs(id BIGSERIAL PRIMARY KEY, user_id BIGINT, vertical_code TEXT, brief_date DATE, items JSONB, channels JSONB, status TEXT);
CREATE TABLE bookmarks(user_id BIGINT, content_id BIGINT, note TEXT, highlights JSONB, PRIMARY KEY(user_id, content_id));
CREATE TABLE api_keys(id BIGSERIAL PRIMARY KEY, owner_user_id BIGINT, key_hash TEXT UNIQUE, prefix TEXT, scopes TEXT[], rate_limit_rpm INT, daily_quota INT, status TEXT);
CREATE TABLE api_usage_daily(key_id BIGINT, day DATE, count INT, PRIMARY KEY(key_id, day));
CREATE TABLE companion_usage(user_id BIGINT, day DATE, count INT, PRIMARY KEY(user_id, day));
```
> L3 只**写**以上表，只**读** L0–L2 的内容/评分/翻译。

---

## 4. 详尽开发 Plan（EPIC → Feature，含验收+自测）

> 里程碑：S1 骨架+展示(E0·E1·E2) → S2 账户+个性化(E3·E4) → S3 伴读+早报(E5·E6) → S4 商业化(E7) → S5 API+MCP+i18n(E8·E9)。预估 ~20 人日。
> MUST=MVP，SHOULD=V1，V2=后置。

### E0 工程基建 (MUST)
- **F0.1 reader-web 脚手架(Next.js)**：App Router+TS+Tailwind+i18n+token。验收：首页 SSR、中英切换占位。自测：E2E 首页 200+语言切换。
- **F0.2 reader-api(FastAPI/JWT) 骨架**：验收：/api 健康、JWT 中间件。自测：受保护路由未授权 401。
- **F0.3 public-api(/v1) 骨架**：验收：/v1 独立前缀+API Key 过滤器占位。自测：无 key 401。
- **F0.4 DB 迁移(L3 表)**：验收：9 张表迁移建出。自测：upgrade/downgrade 往返。

### E1 输入接入 + L2 stub (MUST)
- **F1.1 ContentReadProvider 抽象**：读 COMPLETED+scores+translations。验收：业务只依赖接口。自测：stub 返回 fixture 断言可渲染。
- **F1.2 L2 stub + fixtures**：验收：无真 L2 跑通展示。自测：端到端 stub→列表/详情。

### E2 内容展示 (MUST)
- **F2.1 公共精选列表(SSR/SEO)**：验收：SSR 含内容、评分/来源/缩略图。自测：E2E 断言列表+SEO 标签。
- **F2.2 详情页 + 双语分析**：秒开读 base/scores/translations，中英切换。验收：摘要/观点/金句/六维分展示、语言切换正确。自测：E2E 断言字段+翻译切换。
- **F2.3 统一列表(筛选/排序/分页)**：验收：游标分页无重无漏、筛选排序生效。自测：分页遍历断言总数。

### E3 账户与鉴权 (MUST)
- **F3.1 注册/登录**：邮箱+OAuth(GitHub/Google)。验收：登录闭环、会话持久。自测：E2E 登录流+token 校验。
- **F3.2 门禁 require_plan**：Free/Pro 集中治理。验收：Pro 功能对 Free 拒绝、门禁单点。自测：Free 访问 Pro 路由 403。

### E4 个性化 (SHOULD)
- **F4.1 冷启动兴趣标签**：选 5-8 主题写 user_interests。验收：引导写库。自测：E2E 完成引导断言入库。
- **F4.2 我的早报展示**：读 briefs 或调 L2 recommend。验收：个人早报正确、Pro 门禁。自测：stub 早报断言渲染。
- **F4.3 为你推荐展示**：调 L2 recommend。验收：推荐流渲染。自测：stub recommend 断言列表。
- **F4.4 行为埋点**：点击/深读/收藏/不感兴趣→reading_events。验收：行为入库、北极星可算。自测：模拟行为断言事件+指标。

### E5 AI 伴读 (SHOULD)
- **F5.1 伴读 SSE(前端+代理 L2)**：验收：详情问答流式、reader-api 代理 L2 companion。自测：E2E 断言流式分块到达。
- **F5.2 伴读配额**：companion_usage 计数，Free 限量/Pro 全量。验收：Free 超额拦截、Pro 放行。自测：pytest 超额返回限额提示。

### E6 早报生成 (MUST)
- **F6.1 公共早报 Job**：近 3 天 COMPLETED+加权+去重+质量。验收：生成公共早报、去重生效。自测：构造内容断言早报项与去重。
- **F6.2 我的早报 Job**：逐 Pro 用户。验收：个性化生成。自测：模拟用户断言差异。
- **F6.3 邮件投递**：Resend/SES+打开追踪。验收：发送成功、打开可统计。自测：mock 邮件断言调用。

### E7 商业化 (MUST)
- **F7.1 Paddle/Stripe Checkout(USD)**：$8/月·$79/年(早鸟 $4.9)，Paddle 作 MoR。验收：下单跳转、测试模式成功回流。自测：沙箱下单断言订单创建。
- **F7.2 Webhook + 订阅同步**：验签→更新 subscriptions/plan，门禁实时生效。验收：支付成功升 Pro、退订降级、验签拒伪造。自测：mock webhook 断言 plan 变更、坏签名拒绝。

### E8 Public API + MCP (V2)
- **F8.1 /v1 稳定原语**：today/search/items/trending/sources/verticals，字段裁剪。验收：只读 COMPLETED、响应含 quota、不泄漏内部字段。自测：契约 schema 测试+分页断言。
- **F8.2 API Key + 限流 + 配额**：验收：无效 key 401、超速/超额 429 带重置时间。自测：超速断言 429、配额耗尽拒绝。
- **F8.3 MCP Server**：封装 /v1，`npx @codepick/mcp`。验收：Claude/Cursor 配置后 today/search 可用。自测：MCP 客户端 mock 调用断言结构。

### E9 i18n + SEO (SHOULD)
- **F9.1 中英 i18n**：验收：UI 全量中英、按 Accept-Language/偏好。自测：缺翻译键检测。
- **F9.2 SEO**：SSR+hreflang+结构化数据+sitemap。验收：详情页含 meta/OG/JSON-LD、sitemap 可访问。自测：抓取断言 SEO 标签齐全。

---

## 5. 实施手册（开工就绪）

### 5.1 脚手架
```
apps/reader-web/            # Next.js (App Router, TS, Tailwind, i18n)
  app/(routes) components/ lib/ i18n/
services/reader-api/        # FastAPI 用户API/BFF (JWT)
  routers/{feed,brief,read,companion,events,follow,billing}  auth/{gating,provider,stub}.py
services/public-api/        # FastAPI /v1 (API Key+限流+配额)
services/mcp-server/        # MCP Python SDK
workers/briefs/             # Arq cron: 公共/我的早报 + 邮件
```

### 5.2 关键接口签名
```python
class ContentReadProvider(Protocol):
    def list(self, vertical, status="COMPLETED", filters, cursor, limit) -> Page
    def get(self, content_id) -> ContentDetail   # base+scores+translations
    def recommend(self, user_id, vertical, limit) -> list   # 调 L2
    def companion(self, content_id, question) -> Iterator[str]
class StubContentReadProvider: ...

# BFF（前端唯一入口）
GET /api/feed · /api/brief · /api/read/:id · POST /api/companion(SSE) · /api/events · /api/follow · /api/billing/webhook
def require_plan(plan: str)   # 门禁装饰器/依赖，单点治理

# /v1 对外原语（独立信任边界）
GET /v1/today · /v1/search · /v1/items/:id · /v1/trending · /v1/sources · /v1/verticals
```

### 5.3 配置(.env 追加)
```
JWT_SECRET= OAUTH_GITHUB_ID/SECRET= OAUTH_GOOGLE_ID/SECRET=
PADDLE_VENDOR_ID= PADDLE_API_KEY= PADDLE_WEBHOOK_SECRET=
PRICE_PRO_MONTH_USD=8 PRICE_PRO_YEAR_USD=79 PRICE_EARLYBIRD_USD=4.9
EMAIL_PROVIDER=resend RESEND_API_KEY=
COMPANION_FREE_DAILY=5  API_RATE_FREE_RPM=20  API_QUOTA_FREE_DAY=200
DEFAULT_LOCALE=en SUPPORTED_LOCALES=en,zh  L3_USE_STUB_L2=true
```

### 5.4 冒烟自检（make l3-smoke，L2 stub + 支付沙箱）
```
1. 起 reader-web + reader-api(stub) → 2. E2E 公共精选+详情双语正确
3. E2E 注册登录→兴趣引导→我的早报渲染 → 4. E2E 伴读流式、Free 超额拦截
5. Job 触发早报→断言 briefs 行+邮件 mock 调用 → 6. 沙箱 webhook→断言升 Pro、门禁放行
7. (V2) /v1/today 带 key 返回结构正确、超额 429 → 8. print "L3 PIPELINE: PASS"
```

---

## 6. 测试策略
- **后端单元/集成**：pytest（门禁/分页/配额/webhook 验签），真 PG(testcontainers)。
- **前端 E2E**：Playwright（登录/详情/伴读/支付沙箱），对 L2 stub 数据。
- **契约/SEO**：/v1 响应 schema 校验；SEO 标签断言。

---

## 7. 完成判定（DoD）
- 用 L2 stub + 支付沙箱独立端到端跑通（展示/账户/早报/伴读）
- 公共精选+详情双语正确；统一列表分页稳定
- 账户鉴权闭环；Pro 门禁集中、对 Free 正确拒绝
- 早报生成+邮件；行为埋点支撑北极星
- 计费闭环：升/降级正确、webhook 验签
- (V2) /v1 + Key/限流/配额 + MCP 可用
- i18n 全覆盖、SEO 齐全；E2E + 后端测试 CI 全绿

---

## 8. 共享契约（全平台一致 · 任何层不得擅改）

### 事件主题
| 事件 | 载荷 | 发出 | 消费 |
|---|---|---|---|
| `content.ingested` | `{content_id, lang}` | L0 | L1 |
| `content.analyzed` | `{content_id}` | L1 | L2 |
| `content.completed` | `{content_id}` | L2 | **L3** |
| `content.reprocess` | `{content_id, target_layer}` | 任意 | 对应层 |

### 内容状态机（全局唯一）
`WAIT_FILTER → WAIT_ANALYSIS → WAIT_SCORE → WAIT_REVIEW → COMPLETED | CANCELLED`
**L3 只消费 `COMPLETED`。**

### 表归属（互不重叠）
- L0：sources, ..., content_items, content_versions
- L1：content_base_analysis, enrichment_cache
- L2：verticals, content_vertical_scores, content_translations, review_queue
- **L3：users, subscriptions, user_interests, user_follows, reading_events, briefs, bookmarks, api_keys, api_usage_daily, companion_usage（只读 L0–L2）**

### L3 输入契约（依赖 L2）
```python
class ContentReadProvider(Protocol):
    def list(...) -> Page          # 只读 COMPLETED + scores + translations
    def get(content_id) -> ContentDetail
    def recommend(user_id, vertical, limit) -> list   # 集成期 = L2.recommend
    def companion(content_id, question) -> Iterator[str]  # 集成期 = L2.companion
```
消费的产物：`content_vertical_scores`(VerticalScore) + `content_translations` + base_analysis（均只读）。

### 技术基线（全平台一致）
Python 3.12 / Next.js / PostgreSQL 16 + pgvector / Redis + Arq / SQLAlchemy 2 + Alembic。
开发期：Stub + 支付沙箱；集成期把 StubContentReadProvider 换真实 L2、接真实支付。

---
*CodePick L3 应用与分发 · 开发文档（自包含）· 2026-05-30*
