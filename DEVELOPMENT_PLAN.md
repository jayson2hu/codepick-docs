# CodePick 后续开发计划

更新：2026-09-16。当前事实见[项目进度](PROJECT_STATUS.md)。计划按可交付里程碑组织，不把缺少依据的工期或完成百分比作为承诺。

## M0：恢复环境与契约基线（本轮完成）

- 五仓库同级拉取，Python 3.12 独立环境和 L3 前端依赖恢复。
- 四层已有验证重新执行，修复 L0/L2 事件可靠性和 L3 旧目录问题。
- L1 实际 L0 内容 provider、整数事件、缓存身份和重复状态修复。
- 以新临时数据库和文件存储完成真实 L0 → L1 契约验证。
- 当前进度、跨层缺口、运行入口与本轮验证记录归档。

## M1：L1 持久化与 L1 → L2 数据契约（首版已验收）

依赖：M0。2026-09-12 已完成首版：以下 L1 持久化/事务/版本规则、SQL 适配与独立 L2 读取评分已通过实测。详情见 [M1 记录](M1_INTEGRATION.md)。L0 自动更新通知及 L2 新版重评分仍待接入，未计入首次处理验收。

| 任务 | 交付物 | 验收条件 |
| --- | --- | --- |
| 冻结边界字段 | 版本化 schema/映射及样例，明确数字 ID、`base_tags/tags`、`lang/language`、正文与来源 | 将 L1 实际产物送入 L2 provider；必需字段丢失时报错，禁止默默用空内容代替 |
| L1 SQL 持久化 | L1 所有权内的表与迁移；分析、缓存及状态/成本持久化策略 | 数据库重开/进程重启后仍能读取分析；保留来源内容身份；不写 L2/L3 表 |
| 原子落库与 outbox | 分析/状态/事件的一致提交边界；持久去重与重试键 | 事务失败不产生半份分析或孤立事件；成功提交后队列故障可恢复且不丢通知 |
| 版本与重处理 | 内容版本/graph_version 对去重键、缓存、状态和通知的明确语义 | 同版本重复不新增；版本变化产生新的可消费事件；失败/已完成状态按显式规则重跑 |
| L2 对接真实输出 | 修正 SQL/read adapter，读到正确标签、来源和对象存储正文 | 用 L0 → L1 实际生成的数据跑 L2 评分，校验同一 content_id 和非空来源正文 |

M1 完成的判定：一条本地文章经过实际采集和持久化分析，可被独立 L2 进程读取并评分；重启后结果保留。模型可继续使用 FakeLLM，但记录中必须明确。

## M2：L2 → L3 真实阅读与浏览器连接（首版已验收）

依赖：M1。主要仓库：agentic、pickblog。2026-09-16 已完成首版；详细证据见 [M2 联调记录](M2_INTEGRATION.md)。

| 任务 | 交付物 | 验收条件 |
| --- | --- | --- |
| L2 服务与共享存储 | 状态/成本/outbox 已持久化；继续把 worker 与请求服务接到同一配置 | 独立进程持久结果已验证；服务运行时必须使用同一目标数据库 |
| L2 HTTP 服务 | `/content`、`/content/{id}`、`/recommend`、`/companion` | 对齐 L3 现有分页、详情、推荐和 chunks 约定；缺失 ID 返回404，上游不可用与不存在明确区分 |
| L3 真实 provider | 本地配置、服务鉴权、错误映射 | 关闭 stub 后列表/详情只来自实际 L2 数据；断链呈现可重试错误 |
| 浏览器连接 | 同源代理或允许来源列表；显式演示数据开关 | 不 mock API 的浏览器测试覆盖数据列表和交互；跨域预检通过或完全同源；错误不伪装成演示成功 |

M2 完成的判定：启动本地服务后，可在浏览器读到 M1 文章和评分；停止上游会看到明确错误，重启恢复。不得以 fixture 页面或 mock 浏览器测试代替。

首版验收已满足上述判定：使用保留的 M1 L1/L2 SQLite 数据，依次启动 L2
HTTP、关闭 stub 的 L3 Reader API 和关闭 demo fallback 的 Next.js；Chromium
读取到 M1 文章、六维评分与中文翻译。停止 L2 后页面显示 Retry，恢复服务后
继续读取。PostgreSQL 上的同链路验收、真实模型和持续进程编排仍属于后续。

## 版本与消息闭环（首版已验收）

- L0 同 URL 更新正文后发新版事件，幂等键包含内容版本。
- L1 Redis worker 校验版本并持久分析；outbox relay 保持稳定 run event ID。
- L2 按 run_id 读取对应历史快照，只有更大 revision 能重评分。
- Redis processing 队列支持进程重启恢复，坏信封进入 dead-letter。
- 实际 Redis/Arq 跨进程验证覆盖 v1、v2 和迟到 v1，详见
  [版本消息闭环](VERSIONED_EVENT_LOOP.md)。

剩余：L2 completion outbox 的 sent/ack/dead-letter、PostgreSQL 上同链路、
真实模型成本与长期运行监控。

## M3：公开多用户使用前的门槛

身份与依赖工作可与 M1/M2 并行；本阶段完成前保留本地开发用途。

- 实现真实用户验证、持久用户记录、登录会话和账户隔离。至少验证两个用户的书签、兴趣、订阅与 API key 互不可见，非法/过期令牌被拒绝。
- 升级前端依赖。当前审计 6 项（1 critical、3 high、1 moderate、1 low）；按实际受影响版本选择升级路径，保留锁文件，重跑类型、构建与浏览器回归。
- 将 mock 登录、演示回退、默认 API key/secret 限定在显式开发模式，最终预检对生产误配置失败。
- 校验公开 API 搜索/分页/配额和上游异常映射，不能只在首屏数据中搜索后声称完整搜索。

## M4：分发、收费和真实运行

- **Paddle**：真正的 checkout/交易、price ID、官方 webhook `ts/h1` 签名与时效、事件幂等、订阅状态机；先以沙箱官方协议验证。
- **MCP**：实现协议服务器及 transport，工具经同一 Public API 鉴权/配额/公共字段约束；使用真实 MCP 客户端完成初始化、列工具和调用。
- **队列和邮件**：持久消费/失败重试/可见死信，真实 Redis/Arq 联调；Resend/SES 用独立测试目标验证后再接正式投递。
- **基础设施与模型**：PostgreSQL/pgvector、Redis、对象存储、真实模型提供商及密钥配置；检查数据迁移、重启恢复、可观察性、成本和备份恢复。
- **持续验收**：真实环境全链路 smoke、24 小时 soak、无丢事件和幂等恢复证据。历史独立 DoD PASS 不替代此门槛。

## 每轮交接规则

按用户当轮范围选择一个可完成里程碑，记录基线提交、实际改动、检查结果和明确剩余项。更新本总文档及相关仓库的 `docs/`，保留原始历史记录。所有新验证标明使用真实服务、临时本地存储或模拟实现，避免混用。
