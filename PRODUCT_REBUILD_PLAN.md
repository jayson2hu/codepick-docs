# CodePick 产品改造与验收计划

日期：2026-09-17。状态：本轮 R0/R1 已完成本机验收，R2/R3 为后续计划。产品判断依据见 [产品评审](PRODUCT_REVIEW_2026-09-17.md)，工程事实见 [项目状态](PROJECT_STATUS.md)。

## 本轮交付范围

第一切片交付“公开真实来源 → 可追溯内容 → 可用阅读页面 → 收藏与再访问”，并修复 API 入口的访问歧义。用户希望五仓库重构，本轮按业务链路做有边界的增量改造：每处变化要对应实际场景、现有问题和验收，不为更换目录或重写已通过模块而重构。

本轮可执行：低频获取公开材料、独立本地数据库、无付费模型的显式分析方式、本机服务、无 API mock 浏览器验收、文档与本地/远端代码同步。生产、真实邮件、正式支付、付费模型和真实业务数据库仍不在范围内。

## 架构决策与所有权

沿用五个独立 Git 仓库和 L0–L3 分层。浏览器只访问 L3；L3 通过明确 provider 读取 L2，L2 消费 L1 的版本化快照，L1 从 L0 获取来源内容。跨仓库脚本可编排命令与报告，但不能复制核心业务逻辑或绕过契约写表。

| 仓库 / 层 | 业务职责 | 本轮改造方向 | 禁止越界 |
| --- | --- | --- | --- |
| codepick-docs | 契约、产品决策、跨层验收与环境入口 | 产品评审、问题清单、分阶段计划、真实数据验收记录、启动说明 | 不把文档脚本变成生产采集/分析实现 |
| deepdata / L0 | 公开来源获取、不可变原始内容、规范实体、版本/outbox | 可控公开源导入、来源与时间保留、失败记录、幂等获取 | 不评判垂直业务价值，不导入 L2/L3 |
| seek_data / L1 | 通用清洗、摘要/结构化、语言/标签/向量、持久快照 | 支持真实材料和明确的离线处理方式，保留 provenance 和版本 | 不为特定 UI 合成内容，不伪造模型判断或翻译 |
| agentic / L2 | 垂直判断、评分/翻译/推荐/伴读及读取 API | 暴露可信来源与分析状态，根路径导航；明确未评估/模拟能力 | 不让 L3 重算评分，不把无评估当高质量结果 |
| pickblog / L3 | 阅读、收藏、账户体验、分发和用户数据 | 发现/详情/收藏重构，同源请求，诚实状态反馈，API 导航 | 不跨层直写 L0–L2，不在错误时静默回退 demo |

L0 的 source_url / canonical URL、source identity、published_at / fetched_at、content_version / content_hash 需要沿内容链保留。L1 的输入快照和 run_id/revision 继续是 L2 消费身份；L2 产物状态、分析方式和来源字段只作向后兼容扩展。公共 API 暴露经筛选字段，不泄漏内部数据库地址、凭据、私有推荐上下文或完整异常堆栈。

没有实际模型时，可用明确命名的提取/规则处理保证可读材料，但不能宣称生成了专业摘要、语义向量、中文翻译或经验证的推荐质量。缺字段应显示未知或不可用，不能补造作者/日期/热度。

## 接口与展示契约

| 约束 | 具体要求 |
| --- | --- |
| 服务入口 | Reader API / 和 L2 / 给出服务身份、/docs 与真实健康入口；不伪装为独立用户前端 |
| 远程访问 | 13200 是 Reader Web；18100/18230 是 API。SSH 各转发对应端口，服务仅监听 127.0.0.1 |
| 前端请求 | 浏览器调用同源代理；后端内部调用地址不能烘焙为访问者电脑的 127.0.0.1 |
| 列表/详情 | 使用真实持久 L2 数据；关闭 stub/demo 后错误不得填充假文章 |
| 缺失与故障 | 未知内容 404；无结果 200 空列表；上游暂不可用 503 可重试；配置错误 503 不可重试 |
| 来源展示 | 原文可点击，发布时间和采集时间区分；未知保持未知 |
| 分析展示 | 来源摘录、规则派生、FakeLLM、真实模型的状态清楚；不混作已验证事实 |
| 收藏 | 保存成功后再反馈；本机稍后读和账户收藏明确区分；可再访问，失败时不假成功 |
| 兼容与迁移 | 不删除既有接口、放宽旧契约或破坏版本幂等；数据库只迁移本层表 |

## 阶段与退出条件

### R0：问题核对与计划落地（本轮）

- 保存五仓库分支/HEAD/工作区、适用规范、运行端口和既有数据基线。
- 分别验证 Reader Web、Reader API、L2 API 的根路径、/docs、健康和真实内容端点。
- 完成产品评审、业务边界、优先级和执行分工；修正旧文档断言。
- 退出条件：每个访问问题有“实测事实 / 原因 / 修复 / 复验”记录，不靠猜测。

### R1：真实内容与阅读工作流（本轮主交付）

- 从登记的公开来源低频获取小批量真实内容；每源设置条数、超时和明确失败报告。
- 使用独立本地库完成实际 L0 → L1 → L2 持久链路，保留来源和版本；不得手工把 fixture 直接塞进 L3。
- 调整列表、详情、登录和收藏页面；移动端可用；不虚构用户数、质量评分或同步能力。
- 修复错误提示和服务根路径导航，给出准确的远程访问方法。
- 重复导入验证幂等，重开进程后内容与收藏在其声明范围内可恢复。
- 退出条件：至少一个成功公开源、若干实际文章跨层可追溯；真实浏览器从发现读到原文出处，保存后可再访问；故障不伪装成功。具体条数记录实测，不以造数据满足配额。

### R2：观测、内容运营与稳定性（后续）

- 建立来源登记、展示权利、更新节奏、去重与抽查记录，增加来源失败可见性。
- 实现 v2 事件口径：详情、原文、成功保存、收藏再访问、明确价值反馈；幂等、时间窗口和用户隔离。
- 计算去重周用户漏斗和 cohort，排除测试流量；旧 north_star 字段继续兼容但标注语义。
- 检查保存/同步边界、登录前后状态迁移、重复点击和网络恢复。
- 退出条件：构造有时间和身份的样本可算出预期指标；人工复核能从数字返回原始合法事件；运营报告不混淆未知与 0。

### R3：真实质量与留存验证（后续，依赖授权/样本）

- 经授权后选择真实模型和预算，用真实材料与人工核对样本测试摘要、翻译、判断和引用，不仅检查 HTTP 成功。
- 真实登录/会话生命周期，以及 PostgreSQL 四层同链路、真实 completion 消费者和长期 soak。
- 招募自愿目标用户做任务测试与连续使用，记录反例；联系、邮件和付费调用另按授权执行。
- 退出条件：质量、成本和留存有可追溯数据；商业化方案以实际需求证据收敛，而非把历史价格当结果。

## 并行协作与代码归属

本轮由主代理统筹集成，Astra 承担产品与架构评审，两个 Sol xhigh 子代理分别负责前端和 L0/L1 数据链路；有界子任务并行，模型按用户授权分配。模型名字不是验收依据，所有合并仍以契约、代码审查和实际检查为准。

| 工作流 | 主要写入范围 | 输入 / 输出 |
| --- | --- | --- |
| 产品与文档 | codepick-docs 的 Markdown | 现有资料/代码事实 → 产品评审、实施计划与准确状态 |
| 数据链路 | deepdata、seek_data、agentic 的数据处理与契约相关代码 | 公开来源 → 可重复导入、持久产物、provenance 与测试 |
| 阅读体验 | pickblog/apps/reader-web | 明确真实字段与失败契约 → 发现/详情/收藏 UI 与浏览器检查 |
| 集成与入口 | 主代理协调 API、启动/验收脚本与报告 | 各流改动 → 无 mock 真实演示、错误恢复、文档及 Git 收口 |

同一文件只指定一位主要编辑者；改变 API 字段先通知消费方，改后补契约样例/测试。子代理不独立推送主分支，由主代理审查各仓库 diff、实际测试和最终文档，再按已有授权提交与推送。已有用户修改必须保留。

## 验收矩阵

| 编号 | 场景 | 要保存的证据 |
| --- | --- | --- |
| A01 | 五仓库基线与工作区检查 | 分支、HEAD、remote、dirty 状态及适用规范 |
| A02 | API 根路径/文档/健康可访问 | 对应路径、HTTP 状态、返回服务身份；远程转发说明 |
| A03 | 公开真实来源成功采集 | 来源 URL、抓取时间、成功/失败/去重数量、导入报告路径 |
| A04 | 来源跨层不丢失 | 样本 content_id、版本、L1 run/revision、L2 状态和原文 URL 对照 |
| A05 | 重复导入与进程重启 | 第二次导入差值；重启后内容读取结果 |
| A06 | 无 API mock 浏览器真实阅读 | 实际文章列表、详情、来源链接；运行命令和通过数 |
| A07 | 收藏与再访问 | 保存→收藏页→刷新/重新打开→同条目可读；明确本机/账户模式 |
| A08 | 断开/恢复上游 | 明确可重试错误；恢复后可继续读；保存失败不显示成功 |
| A09 | 双语和移动界面 | 两种语言、常见窄屏、长标题、无日期/无封面、键盘交互 |
| A10 | 按改动运行回归 | 每仓库真实测试数、lint/typecheck/build、失败修复与复跑结果 |
| A11 | 文档与 Git 收口 | 修改清单、完成/未完成、使用的模拟组件、提交/推送结果 |
| A12 | 本机可演示环境 | 精确访问地址、启动方式、数据目录、绑定范围、日志/停止方式 |

A01–A12 的本轮实际结果已写入 [日期化验收记录](PRODUCT_ACCEPTANCE_2026-09-17.md)，仅覆盖该文件列明的本机范围。无法获取的外部源保留失败，不用离线 fixture 冒充在线抓取；生产/凭据事项单独列为未验证。

## 回归与交付规则

Python 固定 3.12，各仓库独立 .venv；Node 20+。先跑直接相关检查，再跑对应仓库完整检查；不能删除测试、降低覆盖率或将失败改为无意义断言。UI 改动要在真实数据和窄屏上检查，API 改动检查 404、错误分类、鉴权边界和旧消费者兼容。

历史 443 项通过是 2026-09-16 基线。本轮新增测试可能改变数量，每份记录注明日期和 commit；不能把旧基线加到本轮未运行检查中充数。真实材料不表示真实模型，真实 PostgreSQL 局部集成不表示生产环境或四层完整闭环。

交付顺序：文档/契约 → 有界实现 → 相关测试 → 完整回归 → 无 mock 浏览器/故障恢复 → 更新进度与证据 → 审查 diff → 按仓库提交并按用户授权推送。测试数据库、原文抓取缓存、令牌和机器特有环境文件不提交。

## 当前进度

| 项目 | 状态（2026-09-17） | 证据/下一步 |
| --- | --- | --- |
| 产品定位、用户任务和运营评审 | 文档已形成 | PRODUCT_REVIEW_2026-09-17.md；仍待真实用户验证 |
| 五层边界、阶段与验收标准 | 文档已形成 | 本文件 |
| API Not Found 原因与导航修复 | 已验收 | API 根路径跳转 /docs，Web 跳转 /zh，同源代理与远程转发说明 |
| 公开真实数据与 provenance | 已验收 | 10 篇采集 / 5 篇可读；L0/L1/L2 重放幂等，版本/摘录/来源一致 |
| 阅读前端与收藏回访 | 已验收 | 双语/移动界面、本机保存与真实账户持久收藏、诚实错误反馈、同页恢复 |
| 本轮完整验收、提交和推送 | 已完成 | PROJECT_STATUS 和日期化记录已同步；五仓库 main 非强制推送 |
