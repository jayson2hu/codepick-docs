# CodePick · L2 判断与智能体 — 开发文档（交给 Codex）

> **用法**：把本文件放进 L2 项目根目录，作为 Codex 的唯一输入。先读完全文，再按「提示词」开工。
> 自包含：提示词 + 产品 + 架构 + DDL + 详尽开发Plan(验收/自测) + 实施手册 + 共享契约。

---

## 0. 给 Codex 的提示词（从这里开始）

**角色**：资深 Python/LangGraph 工程师，独立实现 CodePick 平台的 **L2「判断与智能体」**（垂直评分 + 翻译 + 推荐 + 伴读）。这是护城河层。

**背景**：分层数据平台 **L0→L1→L2→L3**，4 层并行开发、最后联调。你只负责 L2。开发期用 **stub 假装 L1** + **FakeLLM**；联调时换真实 L1/真模型。必须严格遵守第 8 节共享契约。

**任务**：按第 4 节《开发Plan》EPIC E0→E9、Feature 顺序实现，逐个用「验收」+「自测」收口；按第 5 节落地脚手架/接口契约/图规格/配置。

**硬约束**：
1. 独立开发：取 base_analysis 只经 `AnalysisProvider`，注入 `StubAnalysisProvider`+fixtures；LLM 用 `FakeLLM`。**不依赖真 L1、不真调模型。**
2. **标准走配置**：判断标准走 `verticals` 的 lens(rubric/标签体系/source_filter)，**不硬编码**。先做 `ai-coding` 一个 lens。
3. 严守共享契约：`VerticalScore` schema + `content_translations` + 事件 `content.completed` + 表归属（只写 vertical_scores/translations/review_queue，只读 content_items/content_base_analysis）。
4. 评分用"评分→反思(扮演评审专家)→优化"三步。
5. 所有 LLM 节点可用 `FakeLLM` 确定性测试，CI 不真调模型。
6. 栈：Python 3.12 / LangGraph / PostgreSQL+pgvector / Arq。`judgment_graph` 不得 import L3，对 L1 仅经 provider。

**交付标准 (DoD)**：每 Feature 过自测；`make l2-smoke` 输出 `L2 PIPELINE: PASS`；覆盖率≥80%、CI 全绿；满足第 7 节 DoD。产出：代码+测试+迁移+ai-coding lens seed+README。

**工作方式**：每 Sprint 提交一次。契约歧义先提问，不擅改共享契约。

---

## 1. 产品定义

L2 在 L1 的"内容知识"上做**判断与智能处理**：垂直评分、翻译、推荐、伴读。**护城河层**——判断标准（rubric/标签体系/精审）随时间复利，抄不走。L2 是有立场的：回答"对 AI Coding 而言这篇好不好、值不值得推给这个用户"。

- Agent：评分(六维+反思优化)、翻译(中英)、精审(人在环)、推荐/重排、伴读 QA、研究/监控(后期)
- 客户：L3
- 边界：不抓取(L0)、不做通用理解(L1，复用其产物)、不渲染/不计费(L3)
- **垂直 lens**：一个 lens = {源过滤 + 评分 rubric + 标签体系 + 早报模板}；评分是 content×vertical；加垂直=配新 lens 复用同引擎。

---

## 2. 架构设计

```
scoring_graph(vertical): relevance_gate→score_6dim(注入 rubric)→vertical_tag→reflect_check(评审专家)→refine→persist→(review_queue?)→emit content.completed
translate_graph(target_lang): identify_terms→translate→check→refine(意译)→persist
recommend: 召回(标签+向量kNN)→合并去重→rerank(质量×权重+新鲜度−多样性)→topN
companion: load(base+text)→retrieve→answer(SSE)
档位: score/translate=standard, relevance_gate=cheap
缓存: (content_id, vertical, rubric_version)
```

---

## 3. 数据库 DDL（L2 拥有的表）

```sql
CREATE TABLE verticals(
  code TEXT PRIMARY KEY, name TEXT, enabled BOOL DEFAULT true,
  source_filter JSONB DEFAULT '{}', rubric_prompt TEXT, rubric_version TEXT,
  tag_taxonomy JSONB DEFAULT '{}', brief_template TEXT, model_profile JSONB DEFAULT '{}');

CREATE TABLE content_vertical_scores(
  content_id BIGINT REFERENCES content_items(id), vertical_code TEXT REFERENCES verticals(code),
  relevance INT, dim_scores JSONB,   -- {topic,content,depth,practical,novelty,expression}
  vertical_tags JSONB DEFAULT '[]', quality_score INT,
  reviewed BOOL DEFAULT false, review_note TEXT, rubric_version TEXT, model TEXT,
  PRIMARY KEY(content_id, vertical_code));

CREATE TABLE content_translations(
  content_id BIGINT REFERENCES content_items(id), lang TEXT,
  fields JSONB, model TEXT, PRIMARY KEY(content_id, lang));

CREATE TABLE review_queue(
  id BIGSERIAL PRIMARY KEY, content_id BIGINT, vertical_code TEXT,
  reason TEXT, status TEXT DEFAULT 'pending', reviewer TEXT, decided_at TIMESTAMPTZ);

CREATE TABLE user_interest_vectors(  -- 推荐用，L3 触发更新
  user_id BIGINT PRIMARY KEY, vector vector(1536), updated_at TIMESTAMPTZ);
```
> L2 只**写**以上表，只**读** content_items / content_base_analysis。

---

## 4. 详尽开发 Plan（EPIC → Feature，含验收+自测）

> 里程碑：S1 骨架+lens(E0·E1·E2) → S2 评分+翻译(E3·E4) → S3 精审+产物(E5·E8) → S4 推荐+伴读+成本(E6·E7·E9)。预估 ~17 人日。
> MUST=MVP，SHOULD=V1。

### E0 工程基建 (MUST)
- **F0.1 judgment_graph 包 + scoring-worker**：消费 content.analyzed。验收：lint/type 全绿、score 任务可消费。自测：入队断言任务调用。
- **F0.2 复用 LLM 客户端 + 路由**：复用 LLMClient/ModelRouter/FakeLLM(共享 common)。验收：可注入 FakeLLM、走对档位。自测：FakeLLM 注入断言。
- **F0.3 LangGraph 底座 + DB 迁移**：迁移 L2 表。验收：upgrade/downgrade 通过、trace 可观测。自测：迁移往返+示例图 trace。

### E1 输入接入 + L1 stub (MUST)
- **F1.1 AnalysisProvider 抽象**：get(id)->BaseAnalysis。验收：业务只依赖接口。自测：stub 返回 fixture 断言可消费。
- **F1.2 L1 stub + fixtures**：base_analysis 样例(AI Coding/非相关/中英)。验收：无真 L1 跑通。自测：端到端 stub→评分产物。
- **F1.3 消费 content.analyzed**：入队 score，幂等。验收：重复事件不重复评分。自测：双发断言一次。

### E2 垂直 lens (MUST)
- **F2.1 lens 加载器**：按 vertical_code 加载 rubric/taxonomy/filter/model_profile。验收：lens 正确注入图。自测：加载 ai-coding 断言字段。
- **F2.2 ai-coding lens seed**：写六维 rubric + 标签体系 + 源过滤。验收：seed 后 lens 可用、rubric_version 标识。自测：导入 seed 断言 lens 存在。
- **F2.3 relevance_gate 节点**：判断是否属于该垂直，否则跳过。验收：非相关内容正确跳过。自测：相关/不相关样本断言 gate。

### E3 评分图 (MUST)
- **F3.1 score_6dim(注入 rubric)**：relevance + 六维分，结构化 JSON。验收：六维分 0-100 合法、schema 100% 通过。自测：FakeLLM 断言结构；金标一致性。
- **F3.2 vertical_tag**：按 taxonomy 打标。验收：标签 ⊆ taxonomy。自测：断言标签在词表内。
- **F3.3 reflect/check**：扮演领域评审专家查全面/准确/一致。验收：产检查意见与是否需修正。自测：FakeLLM 断言反思结构。
- **F3.4 refine**：据反思优化最终评分/标签。验收：修正后合 schema、记修正原因。自测：注入"需修正"反思断言被采纳。

### E4 翻译图 (MUST)
- **F4.1 identify_terms**：识别专业术语保一致。验收：术语表产出、技术词不乱译。自测：含术语样本断言保留。
- **F4.2 translate+check+refine(中英)**：产 content_translations。验收：双语完整自然、术语一致。自测：FakeLLM 断言 translations 结构；金标自然度抽检。

### E5 精审闭环 (MUST)
- **F5.1 review_queue 入队规则**：低置信(评分边界)/高曝光进精审。验收：命中入队、其余高置信自动 COMPLETED。自测：边界/高分样本断言入队与否。
- **F5.2 人工裁决 + 校准**：→COMPLETED/CANCELLED + 校准反馈。验收：裁决推进状态、可记校准。自测：模拟裁决断言终态。

### E6 推荐/重排 (SHOULD)
- **F6.1 召回(标签+向量)**：标签通道 + pgvector kNN。验收：两路合并去重候选合理。自测：构造用户兴趣断言召回含目标。
- **F6.2 重排**：quality×权重+新鲜度−多样性。验收：排序符合权重、多样性生效。自测：固定输入断言排序确定。
- **F6.3 用户兴趣向量更新**：据行为增量更新。验收：行为后向量更新可解释。自测：模拟行为断言向量变化。

### E7 伴读 QA (SHOULD)
- **F7.1 companion 图(SSE)**：load(base+text)→retrieve→answer 流式。验收：基于内容回答、流式可用。自测：FakeLLM 断言注入上下文+流式分块。
- **F7.2 配额接口(供 L3)**：暴露伴读入口，配额由 L3 治理。验收：接口幂等可计量。自测：调用断言返回+计数钩子。

### E8 持久化与产物契约 (MUST)
- **F8.1 写 content_vertical_scores**：content×vertical upsert。验收：同(content,vertical)一行、字段齐全。自测：评分两次断言一行。
- **F8.2 写 content_translations**：验收：双语入库可被 L3 读。自测：断言中英两行。
- **F8.3 状态推进 + 事件**：WAIT_SCORE→WAIT_REVIEW→COMPLETED；发 content.completed(outbox)。验收：状态合法流转、事件不丢不重。自测：两路(自动/精审)断言终态+事件。

### E9 成本/缓存/重放 (SHOULD)
- **F9.1 rubric_version 重评**：rubric 升级对历史重评(消费 reprocess)。验收：版本升级触发重评、旧分标版本。自测：升级 rubric_version 断言重评。
- **F9.2 成本计量**：验收：评分/翻译成本入库可统计。自测：跑 N 篇断言成本累计。

---

## 5. 实施手册（开工就绪）

### 5.1 脚手架
```
packages/judgment_graph/judgment_graph/
  lens/loader.py  graph/{scoring,translate,recommend,companion,state,build}.py
  input/{provider,stub}.py  persist/{repository,contracts}.py  review/queue.py  events/{consume,emit}.py
  scripts/smoke.py  tests/{unit,graph,fixtures/{analysis,llm},golden}
workers/scoring/worker.py
```

### 5.2 关键接口签名
```python
class AnalysisProvider(Protocol):
    def get(self, content_id) -> BaseAnalysis: ...   # summary/key_points/entities/embedding
class StubAnalysisProvider: ...   # fixtures

class Lens: code; rubric_prompt; rubric_version; tag_taxonomy; source_filter; model_profile
def load_lens(vertical_code) -> Lens

class VerticalScore(BaseModel):
    content_id:int; vertical_code:str; relevance:int
    dim_scores:dict   # topic/content/depth/practical/novelty/expression
    vertical_tags:list[str]; quality_score:int; rubric_version:str

# 产物契约（给 L3，冻结，见第 8 节）
def persist_score(s: VerticalScore) -> None              # upsert (content,vertical)
def persist_translation(content_id, lang, fields) -> None
def emit_completed(session, content_id) -> None           # outbox: content.completed
def recommend(user_id, vertical, limit) -> list[ContentRef]
def companion(content_id, question) -> Iterator[str]      # SSE
```

### 5.3 配置(.env 追加)
```
L2_DEFAULT_VERTICAL=ai-coding  L2_RUBRIC_VERSION=aic-v1
L2_REVIEW_LOWCONF_BAND=10  L2_RECO_VECTOR_ENABLED=true
L2_USE_STUB=true  L2_USE_FAKE_LLM=true
```

### 5.4 冒烟自检（make l2-smoke，FakeLLM）
```
1. Stub 提供 base_analysis(AI Coding 文章)→score(cid, ai-coding)
2. assert content_vertical_scores 六维分合法 → 3. assert content_translations 中英两行
4. assert status==COMPLETED(高置信)/入 review_queue(低置信) → 5. assert outbox content.completed
6. recommend(user) 非空排序确定；companion 流式返回 → 7. print "L2 PIPELINE: PASS"
```

---

## 6. 测试策略
- **单元**：重排打分/标签校验/召回合并，覆盖率≥80%。
- **图测试(FakeLLM)**：评分/翻译/伴读图确定性测试，断言结构化产物+状态流转。**CI 不真调模型。**
- **质量/金标**：评分一致性(AI vs 精审)、翻译自然度离线评估。

---

## 7. 完成判定（DoD）
- 用 L1 stub + FakeLLM 独立跑通验收
- 六维评分合法 + 反思优化 + 与精审一致性达标
- 中英翻译完整自然、术语一致
- 精审闭环：低置信入队、裁决推进、可校准
- 推荐两路召回+重排；伴读基于内容流式
- 产物契约完整；content.completed 不丢不重；状态正确
- 成本可控可重评；覆盖率达标、CI 全绿(不真调模型)

---

## 8. 共享契约（全平台一致 · 任何层不得擅改）

### 事件主题
| 事件 | 载荷 | 发出 | 消费 |
|---|---|---|---|
| `content.ingested` | `{content_id, lang}` | L0 | L1 |
| `content.analyzed` | `{content_id}` | L1 | **L2** |
| `content.completed` | `{content_id}` | **L2** | L3 |
| `content.reprocess` | `{content_id, target_layer}` | 任意 | 对应层 |

### 内容状态机（全局唯一）
`WAIT_FILTER → WAIT_ANALYSIS → WAIT_SCORE → WAIT_REVIEW → COMPLETED | CANCELLED`
**L2 负责 `WAIT_SCORE→WAIT_REVIEW→COMPLETED` / `CANCELLED`。** 前台/对外只消费 COMPLETED。

### 表归属（互不重叠）
- L0：sources, ..., content_items, content_versions
- L1：content_base_analysis, enrichment_cache（只读 content_items）
- **L2：verticals, content_vertical_scores, content_translations, review_queue（只读 content_items, content_base_analysis）**
- L3：users, subscriptions, ...（只读 L0–L2）

### L2 产物契约（冻结，L3 依赖）
```python
class VerticalScore(BaseModel):
    content_id:int; vertical_code:str; relevance:int
    dim_scores:dict; vertical_tags:list[str]; quality_score:int; rubric_version:str
# content_translations(content_id, lang, fields)
# 请求时: recommend(user_id, vertical, limit) / companion(content_id, question)->SSE
```
### L2 输入契约（依赖 L1）
```python
class AnalysisProvider(Protocol):
    def get(self, content_id) -> BaseAnalysis   # 集成期 = L1 产物
```

### 技术基线（全平台一致）
Python 3.12 / PostgreSQL 16 + pgvector / Redis + Arq / SQLAlchemy 2 + Alembic / LangGraph。
开发期：Stub + FakeLLM；集成期把 StubAnalysisProvider 换真实 L1。

---
*CodePick L2 判断与智能体 · 开发文档（自包含）· 2026-05-30*
