# Codex 任务：实现 CodePick · L1 数据加工（独立开发，最后联调）

## 角色
你是资深 Python/LangGraph 工程师，独立负责实现 CodePick 平台的 **L1「数据加工」**：把原始内容加工成与垂直无关的结构化"内容知识"（摘要/观点/金句/实体/向量/基础标签）。

## 背景（重要）
CodePick 是分层数据平台：**L0 数据底座 → L1 数据加工 → L2 判断与智能体 → L3 应用与分发**。
4 层由不同会话**并行开发，最后统一联调**。你只负责 L1。开发期你用 **stub 假装 L0**；联调时会把 stub 换成真实 L0。因此你必须**严格遵守输入/输出契约**。

## 输入材料（本文件夹 4 份 HTML，先全部读完再动手）
- `L1-数据加工-产品文档.html` —— 目标、"与垂直无关·算一次"原则、产物契约、边界
- `L1-数据加工-架构设计.html` —— LangGraph 基础分析图、数据模型、分级、缓存/重放
- `L1-开发Plan-详尽版.html` —— EPIC/Feature 拆解 + 每项「验收标准」+「自测」（实现依据）
- `L1-实施手册-交付包.html` —— 脚手架、接口契约、LangGraph 图规格、配置、冒烟自检

## 任务
按《开发Plan》的 EPIC **E0→E9**、Feature 顺序实现，**逐个用其「验收标准」+「自测」收口**；
按《实施手册》的脚手架、接口契约、LangGraph 图规格、配置落地。

## 硬约束（不可违背）
1. **独立开发**：取内容只经 `ContentProvider` 接口，注入 `StubContentProvider` + fixtures；LLM 用 `FakeLLM`。**不依赖真 L0、不真调模型。**
2. **与垂直无关**：只做通用理解，**绝不**含"对某垂直好不好/值不值得读"的判断（那是 L2）。
3. **冻结契约**（上层 L2 唯一依赖，不可改）：
   - 输入：事件 `content.analyzed` 的触发；`ContentProvider.get(id) -> ContentInput`
   - 输出：产物 `BaseAnalysis`（one_liner/summary/key_points/quotes/entities/base_tags/embedding/lang）+ 事件 `content.analyzed {content_id}`
   - 表归属：L1 **只写** `content_base_analysis / enrichment_cache`，**只读** `content_items`
   - 状态推进：`WAIT_ANALYSIS → WAIT_SCORE`（成功）/ `CANCELLED`（初评剔除）
4. **成本**：小模型初评短路 + 缓存 + 并发限流；模型档位走 ConfigKey。
5. **可测性**：所有 LLM 节点必须能用 `FakeLLM` 确定性测试，CI **不真调模型、不花钱**。
6. **依赖方向**：`enrichment_graph` 不得 import L2/L3，对 L0 仅经 `ContentProvider` 接口。
7. **技术栈**：Python 3.12 / LangGraph / PostgreSQL + pgvector / SQLAlchemy + Alembic / Arq。

## 交付标准（DoD）
- 每个 Feature 通过其「自测」；每个 Sprint 末 `make l1-smoke` 输出 `L1 PIPELINE: PASS`。
- 覆盖率 ≥ 80%，ruff / mypy / CI 全绿。
- 满足《实施手册》第 8 节 L1 DoD 全部条目（含：stub 独立跑通、抽取合 schema、长文不遗漏、缓存命中省成本）。
- 产出：可运行代码 + 测试（FakeLLM + fixtures）+ Alembic 迁移 + README。

## 工作方式
- 每完成一个 Sprint 提交一次，附：已实现 Feature、自测结果、覆盖率、遗留项。
- **联调预留**：把 `StubContentProvider` 与 `L0ContentProvider` 设计成可一键切换；冻结契约保持稳定。
- 契约歧义**先停下提问**，不擅改冻结契约。
