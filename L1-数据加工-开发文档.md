# CodePick · L1 数据加工 — 开发文档（交给 Codex）

> **用法**：把本文件放进 L1 项目根目录，作为 Codex 的唯一输入。先读完全文，再按「提示词」开工。
> 自包含：提示词 + 产品 + 架构 + DDL + 详尽开发Plan(验收/自测) + 实施手册 + 共享契约。

---

## 0. 给 Codex 的提示词（从这里开始）

**角色**：资深 Python/LangGraph 工程师，独立实现 CodePick 平台的 **L1「数据加工」**（清洗 + 初评 + 基础分析 + 向量）。

**背景**：分层数据平台 **L0→L1→L2→L3**，4 层并行开发、最后联调。你只负责 L1。开发期用 **stub 假装 L0** + **FakeLLM**；联调时换真实 L0/真模型。必须严格遵守第 8 节共享契约。

**任务**：按第 4 节《开发Plan》EPIC E0→E9、Feature 顺序实现，逐个用「验收」+「自测」收口；按第 5 节落地脚手架/接口契约/LangGraph 图规格/配置。

**硬约束**：
1. 独立开发：取内容只经 `ContentProvider`，注入 `StubContentProvider`+fixtures；LLM 用 `FakeLLM`。**不依赖真 L0、不真调模型。**
2. **与垂直无关**：只做通用理解（摘要/观点/金句/实体/向量/基础标签），**绝不**含"对某垂直好不好"的判断（那是 L2）。
3. 严守共享契约：产物 `BaseAnalysis` schema + 事件 `content.analyzed` + 表归属（只写 content_base_analysis/enrichment_cache，只读 content_items）。
4. 成本：小模型初评短路 + 缓存 + 并发限流；模型档位走 ConfigKey。
5. 所有 LLM 节点可用 `FakeLLM` 确定性测试，CI 不真调模型、不花钱。
6. 栈：Python 3.12 / LangGraph / PostgreSQL+pgvector / SQLAlchemy+Alembic / Arq。`enrichment_graph` 不得 import L2/L3，对 L0 仅经 provider。

**交付标准 (DoD)**：每 Feature 过自测；`make l1-smoke` 输出 `L1 PIPELINE: PASS`；覆盖率≥80%、CI 全绿；满足第 7 节 DoD。产出：代码+测试+迁移+README。

**工作方式**：每 Sprint 提交一次。契约歧义先提问，不擅改共享契约。

---

## 1. 产品定义

L1 把 L0 的原始内容加工成**结构化"内容知识"**，**与垂直无关、算一次、全局复用**——上层一切判断与体验的共同底料。把"理解成本"从 N 次降到 1 次（成本优势 + 跨垂直综合能力来源）。

- 能力：清洗+初评过滤、基础分析(一句话/摘要/关键观点/金句/实体)、embedding、语言识别、基础打标
- 客户：L2(评分/翻译输入)、L3(详情秒开/搜索)
- 边界：不做垂直判断/翻译/推荐(L2)，不抓取(L0)

---

## 2. 架构设计（LangGraph 基础分析图）

```
enrichment_graph:
  clean_normalize → filter(cheap LLM, 低质→CANCELLED 短路) → detect_lang
    → branch by length: short→analyze_full | long→split→map(analyze_chunk)→aggregate
    → extract(one_liner/summary/key_points/quotes/entities)
    → embed(→pgvector) → base_tag → persist → emit content.analyzed
```
- **模型分级**：filter=小模型、analyze/extract=中端、embed=embedding；走 ConfigKey。
- **缓存/幂等**：按 (content_hash, graph_version)；命中跳过 LLM。
- **失败**：节点级重试；schema 校验失败→重试/降级；不阻塞发布。

---

## 3. 数据库 DDL（L1 拥有的表）

```sql
CREATE TABLE content_base_analysis(
  content_id BIGINT PRIMARY KEY REFERENCES content_items(id),
  one_liner TEXT, summary TEXT,
  key_points JSONB DEFAULT '[]', quotes JSONB DEFAULT '[]',
  entities JSONB DEFAULT '[]', base_tags JSONB DEFAULT '[]',
  embedding vector(1536), lang TEXT,
  model TEXT, graph_version TEXT,
  tokens_in INT, tokens_out INT, cost NUMERIC(10,5),
  created_at TIMESTAMPTZ DEFAULT now());
CREATE INDEX ON content_base_analysis USING ivfflat (embedding vector_cosine_ops);

CREATE TABLE enrichment_cache(
  content_hash TEXT, graph_version TEXT, result JSONB,
  created_at TIMESTAMPTZ DEFAULT now(), PRIMARY KEY(content_hash, graph_version));
```
> L1 只**写**这两张表，只**读** `content_items`（L0 拥有）。

---

## 4. 详尽开发 Plan（EPIC → Feature，含验收+自测）

> 里程碑：S1 骨架(E0·E1·E2) → S2 核心分析(E3·E4·E5) → S3 向量+产物(E6·E7) → S4 成本+重放(E8·E9)。预估 ~17 人日。
> 优先级 MUST=MVP，SHOULD=V1。

### E0 工程基建 (MUST)
- **F0.1 包骨架 + enrichment worker**：Arq 消费 enrich(content_id)。验收：lint/type 全绿、任务可消费。自测：入队断言任务调用。
- **F0.2 LLM 客户端 + 多模型路由**：`LLMClient`(complete/structured/embed) + `ModelRouter`(tier→model, ConfigKey) + `FakeLLM`。验收：切 tier 路由对应模型；FakeLLM 可替换。自测：注入 FakeLLM 断言响应 + 路由表。
- **F0.3 LangGraph 运行底座**：GraphState 基类 + 节点装饰器(计时/计费/错误) + trace。验收：2 节点示例图可跑并产 trace。自测：跑示例图断言 trace。

### E1 输入接入 + L0 stub (MUST)
- **F1.1 ContentProvider 抽象**：get(id)->ContentInput；真实现调 L0，stub 读 fixtures。验收：业务只依赖接口。自测：stub 返回 fixture 断言可消费。
- **F1.2 L0 stub + fixtures**：5-10 篇样例(超长/营销/中英)。验收：无真 L0 跑通 L1。自测：端到端 stub→产物。
- **F1.3 事件消费**：订阅 content.ingested→入队 enrich，幂等。验收：重复事件不重复加工。自测：双发事件断言一次。

### E2 管线骨架 (MUST)
- **F2.1 GraphState 定义**：content_id/text/lang/中间产物/计费。验收：可序列化、节点间传递。自测：跑两节点断言字段累积。
- **F2.2 图装配 + enrich() 入口**：组装占位节点链 + graph_version。验收：enrich 端到端跑通(占位)产空壳产物。自测：stub→enrich→落产物行。

### E3 清洗与初评过滤 (MUST)
- **F3.1 clean_normalize**：去噪/分段/长度评估。验收：HTML 残留清理，输出长度统计。自测：脏文本断言清洗+段落数。
- **F3.2 filter 初评(小模型)**：CO-STAR 风格 prompt(中/英)，{ignore,reason,value}；ignore→CANCELLED 短路。验收：营销/低质剔除，正常通过，短路不进昂贵节点。自测：FakeLLM 低质断言 CANCELLED、正常断言继续；金标过滤准确率达标。

### E4 长文分支与分段聚合 (SHOULD)
- **F4.1 长度分支**：阈值(如 6000)。验收：长/短走对分支，阈值可配。自测：长短样本断言分支。
- **F4.2 分段 map + 聚合**：split→map analyze_chunk→aggregate。验收：长文聚合覆盖各段要点不遗漏不重复。自测：FakeLLM 多段断言聚合含各段标志要点。

### E5 基础分析抽取 (MUST)
- **F5.1 结构化抽取**：one_liner/summary/key_points/quotes/entities，JSON schema 约束(中/英)。验收：严格合 schema、字段非空(quotes 可空)、与原文一致不臆造。自测：schema 100% 通过；金标抽检准确性。
- **F5.2 校验与降级重试**：非法 JSON→重试/降级。验收：非法触发重试；多次失败兜底不污染产物。自测：FakeLLM 返回坏 JSON 断言重试与处理。

### E6 向量与基础打标 (MUST)
- **F6.1 Embedding**：title+summary→embedding→pgvector。验收：维度正确写入；相似内容向量邻近。自测：相似/不相似断言余弦相似度排序。
- **F6.2 语言识别 + 基础标签**：lang + 通用主题/形式标签(**与垂直无关**)。验收：lang 准确；标签来自通用体系不含垂直判断。自测：中/英断言 lang；标签 ⊆ 通用词表。

### E7 持久化与产物契约 (MUST)
- **F7.1 写 content_base_analysis**：含 model/graph_version/tokens/cost，按 content_id upsert。验收：完整入库；重复不产生重复行。自测：加工两次断言一行、字段齐全。
- **F7.2 状态推进**：WAIT_ANALYSIS→WAIT_SCORE(成功)/CANCELLED(剔除)。验收：流转合状态机；非法跃迁被拒。自测：成功/剔除两路断言终态。
- **F7.3 content.analyzed 事件(outbox)**：事务内写 outbox。验收：每条完成恰发一次，不丢不重。自测：完成断言 outbox 有事件；relay 后标记。

### E8 模型分级与成本 (MUST)
- **F8.1 模型路由(分档)**：filter=cheap/analyze=standard/embed=embed，ConfigKey 切换。验收：各节点用对档模型；改配置即切。自测：断言各节点请求模型档位。
- **F8.2 成本计量与告警**：记 tokens/cost，超阈告警。验收：每篇 cost 入库；日成本可统计；超阈告警。自测：跑 N 篇断言 cost 累计与告警。

### E9 缓存/幂等/重放 (SHOULD)
- **F9.1 enrichment_cache**：(content_hash, graph_version) 命中复用。验收：相同内容+版本第二次命中，无 LLM 调用。自测：同内容跑两次断言第二次 LLM 调用=0。
- **F9.2 重放(reprocess)**：消费 content.reprocess 或 graph_version 升级重跑。验收：触发重放重新加工；版本升级使旧缓存失效。自测：升级 graph_version 断言重新加工。

---

## 5. 实施手册（开工就绪）

### 5.1 环境
复用平台 docker-compose(postgres+pgvector/redis)。Make 目标：`up/migrate/l1-enrich CID=/l1-smoke/test`。测试用 FakeLLM 不需真凭证。

### 5.2 脚手架
```
packages/enrichment_graph/enrichment_graph/
  config.py  llm/{client,router,fake}.py
  graph/{state,build}.py  graph/nodes/{clean,filter,split,analyze,extract,embed,tag,persist}.py
  input/{provider,stub}.py  persist/{repository,contracts}.py  events/{consume,emit}.py
  cache.py cost.py scripts/smoke.py
  tests/{unit,graph,fixtures/{content,llm},golden}
workers/enrichment/worker.py
```

### 5.3 关键接口签名
```python
class ContentInput(BaseModel):
    id:int; clean_text:str; title:str|None; lang:str|None; source:SourceRef; published_at; content_hash:str
class ContentProvider(Protocol):
    def get(self, content_id) -> ContentInput: ...
class StubContentProvider: ...   # fixtures
class L0ContentProvider: ...      # 调 L0 get_content

class LLMClient(Protocol):
    def complete(self, prompt, *, tier) -> str: ...
    def structured(self, prompt, schema, *, tier) -> dict: ...
    def embed(self, text) -> list[float]: ...
class ModelRouter: tiers = {"cheap","standard","embed"}
class FakeLLM(LLMClient): ...

class GraphState(TypedDict):
    content_id; text; lang; ignore; chunks
    one_liner; summary; key_points; quotes; entities; base_tags; embedding
    tokens_in; tokens_out; cost; graph_version
def node(state: GraphState, *, llm: LLMClient) -> GraphState: ...

# 产物契约（给 L2，冻结，见第 8 节）
class BaseAnalysis(BaseModel):
    content_id:int; one_liner:str; summary:str
    key_points:list[str]; quotes:list[str]; entities:list[Entity]; base_tags:list[str]; lang:str
def persist(analysis: BaseAnalysis, meta) -> None     # upsert by content_id
def emit_analyzed(session, content_id) -> None        # outbox: content.analyzed
```

### 5.4 图规格
```
build_graph(graph_version="l1-v1"):
  clean → filter(ignore?→END:CANCELLED) → branch_by_length
    short→analyze_full | long→split→map(analyze_chunk)→aggregate
    → extract → embed → tag → persist → emit
档位: filter=cheap, analyze/extract=standard, embed=embed
缓存键: (content_hash, graph_version)
```

### 5.5 配置(.env 追加)
```
LLM_API_BASE= LLM_API_KEY=
LLM_TIER_CHEAP= LLM_TIER_STANDARD= LLM_TIER_EMBED=  EMBED_DIM=1536
L1_LONG_TEXT_THRESHOLD=6000  L1_GRAPH_VERSION=l1-v1  L1_FILTER_ENABLED=true
L1_COST_DAILY_ALERT_USD=20  L1_MAX_CONCURRENCY=4  L1_USE_STUB=true  L1_USE_FAKE_LLM=true
```

### 5.6 冒烟自检（make l1-smoke，FakeLLM）
```
provider=StubContentProvider; llm=FakeLLM
1. fixture(正常文章)→enrich → 2. assert base_analysis 存在合 schema
3. assert summary/key_points/entities 非空、embedding 维度=EMBED_DIM → 4. assert status==WAIT_SCORE
5. fixture(营销低质)→enrich → assert status==CANCELLED(短路)
6. 同内容再 enrich → assert LLM 调用=0(缓存) → 7. assert outbox content.analyzed
8. print "L1 PIPELINE: PASS"
```

---

## 6. 测试策略
- **单元**：清洗/分段/schema 校验/标签映射/成本计算，覆盖率≥80%。
- **图测试**：用 **FakeLLM/录制回放** 提供确定性响应，对 fixture 跑整图断言结构化产物。**CI 不真调模型、不花钱。**
- **质量/金标**：人工标注摘要/实体离线评估准确率；过滤准确率/抽取覆盖率阈值。

---

## 7. 完成判定（DoD）
- 用 stub + FakeLLM 独立端到端跑通验收，无需真 L0/真模型
- 初评剔除低质(CANCELLED)；正常文章产完整结构化分析
- 抽取严格合 BaseAnalysis schema、与原文一致
- 长文分段聚合不遗漏；embedding 入 pgvector；标签与垂直无关
- content_base_analysis 完整 upsert；content.analyzed 不丢不重；状态正确
- 模型分级生效、单篇成本可控可统计、超阈告警
- 缓存命中省成本、版本升级可重放
- 单元+图测试覆盖率达标、CI 全绿(不真调模型)

---

## 8. 共享契约（全平台一致 · 任何层不得擅改）

### 事件主题
| 事件 | 载荷 | 发出 | 消费 |
|---|---|---|---|
| `content.ingested` | `{content_id, lang}` | L0 | **L1** |
| `content.analyzed` | `{content_id}` | **L1** | L2 |
| `content.completed` | `{content_id}` | L2 | L3 |
| `content.reprocess` | `{content_id, target_layer}` | 任意 | 对应层 |

### 内容状态机（全局唯一）
`WAIT_FILTER → WAIT_ANALYSIS → WAIT_SCORE → WAIT_REVIEW → COMPLETED | CANCELLED`
**L1 负责 `WAIT_ANALYSIS→WAIT_SCORE`(成功) / `CANCELLED`(初评剔除)。**

### 表归属（互不重叠）
- L0：sources, source_health, crawl_jobs, raw_documents, content_items, content_versions
- **L1：content_base_analysis, enrichment_cache（只读 content_items）**
- L2：verticals, content_vertical_scores, content_translations, review_queue（只读 content_items, content_base_analysis）
- L3：users, subscriptions, ...（只读 L0–L2）

### L1 产物契约（冻结，L2 依赖）
```python
class BaseAnalysis(BaseModel):
    content_id:int; one_liner:str; summary:str
    key_points:list[str]; quotes:list[str]; entities:list[Entity]; base_tags:list[str]; lang:str
# embedding 存 pgvector(content_base_analysis.embedding)，不在对外 schema
```
### L1 输入契约（依赖 L0）
```python
class ContentProvider(Protocol):
    def get(self, content_id) -> ContentInput   # 集成期 = L0.get_content
```

### 技术基线（全平台一致）
Python 3.12 / PostgreSQL 16 + pgvector / Redis + Arq / SQLAlchemy 2 + Alembic / LangGraph。
开发期：Stub + FakeLLM，CI 不真调模型；集成期把 StubContentProvider 换 L0ContentProvider。

---
*CodePick L1 数据加工 · 开发文档（自包含）· 2026-05-30*
