# CodePick · L0 数据底座 — 开发文档（交给 Codex）

> **用法**：把本文件放进 L0 项目根目录，作为 Codex 的唯一输入。先读完全文，再按「提示词」开工。
> 本文件自包含：提示词 + 产品 + 架构 + DDL + 详尽开发Plan(验收/自测) + 实施手册 + 共享契约。

---

## 0. 给 Codex 的提示词（从这里开始）

**角色**：资深 Python 数据工程师，独立实现 CodePick 平台的 **L0「数据底座」**（采集 + 完整保存 + 规范化 + 去重 + 事件）。

**背景**：CodePick 是分层数据平台 **L0→L1→L2→L3**，4 层并行开发、最后联调。你只负责 L0。其他层以 stub 对接你，因此你必须严格遵守第 8 节「共享契约」。

**任务**：按第 4 节《开发Plan》EPIC E0→E8、Feature 顺序实现，逐个用其「验收」+「自测」收口；按第 5 节《实施手册》脚手架、DDL、接口契约、docker-compose 落地。

**硬约束**：
1. 信条：尽可能保存所有信息；原始层 `raw_documents` 不可变、只追加、永不删除；`RawStore` 仓储**无 update/delete**。
2. 三段存储（原始/规范/索引）+ 强血缘；可重放（从原始层重建规范层，无需重抓）。
3. 去重保留变体：去重只选代表，原始抓取各自留痕。
4. 事件用事务性 outbox，`content.ingested` 不丢不重。
5. 严守第 8 节共享契约（事件/表归属/查询契约）；`core_data` 不得 import 上层。
6. 栈：Python 3.12 / Playwright / feedparser / PostgreSQL+pgvector / SQLAlchemy+Alembic / Redis+Arq / S3 兼容。

**交付标准 (DoD)**：每 Feature 过自测；`make smoke` 输出 `L0 PIPELINE: PASS`；覆盖率≥80%、CI 全绿；满足第 7 节 DoD。产出：代码+测试+迁移+docker-compose+README。

**工作方式**：每 Sprint 提交一次（已实现 Feature/自测结果/覆盖率/遗留项）。契约歧义先提问，不擅改共享契约。

---

## 1. 产品定义

L0 是独立数据产品：把外部内容采集进来并**尽可能完整、不可变地保存**，成为平台唯一可信数据真相源，为上层提供数据驱动。**不做任何业务判断**（评分/推荐是 L2，摘要/实体是 L1）。

- **采集一切**：RSS、网页全文(无头浏览器)、播客、X
- **完整保存**：原始 entry、原始 HTML、抓取元数据、媒体、版本——全留存不裁剪
- **可重放驱动**：上层算法升级时，从原始层重跑，无需再抓
- **客户**：L1/L2/L3（下层服务上层）
- **边界**：不感知"垂直"，只知"内容与源"；不渲染界面、不管账户

---

## 2. 架构设计

**三段式存储**（核心）：
- **① 原始层 RAW**（不可变、只追加）：源头原始条目 + 原始 HTML + HTTP 头 + 媒体；大块进对象存储，库存指针+sha256。可重放与零丢失的根基。
- **② 规范层 CANONICAL**（派生、可重建）：统一 ContentItem（清洗正文/标题/语言/状态）。上层主消费。
- **③ 索引层 INDEX**（可重建）：去重指纹、源健康、抓取日志。

**组件**：源注册表 → 调度器 → (RSS Poller / Web Fetcher / Media Fetcher) → 正文提取 → 去重 → 写三层 → 发 `content.ingested`。

**为什么分三段**：原始层保证存全不丢，规范层保证上层好用，索引层保证查得快；派生层永远能从原始层重建。

---

## 3. 数据库 DDL（可执行）

```sql
CREATE EXTENSION IF NOT EXISTS vector;

CREATE TABLE sources(
  id BIGSERIAL PRIMARY KEY, name TEXT NOT NULL, feed_url TEXT, home_url TEXT,
  type TEXT NOT NULL CHECK (type IN ('article','podcast','video','tweet')),
  crawl_config JSONB NOT NULL DEFAULT '{}', etiquette JSONB NOT NULL DEFAULT '{}',
  vertical_codes TEXT[] NOT NULL DEFAULT '{}',
  is_public BOOL NOT NULL DEFAULT true, owner_user_id BIGINT,
  enabled BOOL NOT NULL DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now());

CREATE TABLE source_health(
  source_id BIGINT PRIMARY KEY REFERENCES sources(id),
  last_ok_at TIMESTAMPTZ, last_attempt_at TIMESTAMPTZ,
  fail_count INT DEFAULT 0, empty_streak INT DEFAULT 0,
  status TEXT DEFAULT 'unknown', note TEXT);

CREATE TABLE crawl_jobs(
  id BIGSERIAL PRIMARY KEY, source_id BIGINT REFERENCES sources(id),
  kind TEXT NOT NULL, started_at TIMESTAMPTZ DEFAULT now(), finished_at TIMESTAMPTZ,
  status TEXT DEFAULT 'running', stats JSONB DEFAULT '{}', error TEXT);

-- ① 原始层（不可变，只追加）
CREATE TABLE raw_documents(
  id BIGSERIAL PRIMARY KEY, source_id BIGINT REFERENCES sources(id),
  crawl_job_id BIGINT REFERENCES crawl_jobs(id),
  url TEXT NOT NULL, fetch_method TEXT NOT NULL,
  http_status INT, http_headers JSONB, fetched_at TIMESTAMPTZ DEFAULT now(),
  raw_entry JSONB, raw_html_ref TEXT, raw_html_sha256 TEXT,
  media_refs JSONB DEFAULT '[]', extra JSONB DEFAULT '{}');
CREATE INDEX ON raw_documents(source_id, fetched_at);
CREATE INDEX ON raw_documents(raw_html_sha256);

-- ② 规范层（派生，可重建，上层主消费）
CREATE TABLE content_items(
  id BIGSERIAL PRIMARY KEY,
  canonical_url TEXT NOT NULL, url_hash TEXT NOT NULL UNIQUE,
  content_hash TEXT, simhash BIGINT,
  title TEXT, author TEXT, clean_text_ref TEXT, lang TEXT,
  source_id BIGINT REFERENCES sources(id), published_at TIMESTAMPTZ,
  fetched_at TIMESTAMPTZ DEFAULT now(), current_version INT DEFAULT 1,
  raw_document_id BIGINT REFERENCES raw_documents(id),
  status TEXT NOT NULL DEFAULT 'WAIT_FILTER',
  meta JSONB DEFAULT '{}', created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now());
CREATE INDEX ON content_items(status, fetched_at);
CREATE INDEX ON content_items(source_id, published_at);
CREATE INDEX ON content_items(content_hash);

CREATE TABLE content_versions(
  id BIGSERIAL PRIMARY KEY, content_id BIGINT REFERENCES content_items(id),
  version INT NOT NULL, raw_document_id BIGINT REFERENCES raw_documents(id),
  clean_text_ref TEXT, content_hash TEXT, diff_summary TEXT,
  detected_at TIMESTAMPTZ DEFAULT now(), UNIQUE(content_id, version));

-- ③ 事件发件箱（事务性 outbox）
CREATE TABLE outbox_events(
  id BIGSERIAL PRIMARY KEY, topic TEXT NOT NULL, payload JSONB NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now(), published_at TIMESTAMPTZ);
CREATE INDEX ON outbox_events(published_at) WHERE published_at IS NULL;
```

---

## 4. 详尽开发 Plan（EPIC → Feature，含验收+自测）

> 优先级：**MUST** = MVP 必需；**SHOULD** = V1 完善。先做完所有 MUST 即得可运行的数据底座。
> 里程碑：S1 基建(E0) → S2 源+RSS(E1·E2·E3) → S3 抓取+去重(E4·E5) → S4 原始层+事件(E6·E7)。预估 ~23 人日。

### E0 工程基建 (MUST)
- **F0.1 仓库骨架与依赖**：uv workspace + ruff/mypy/import-lint(禁 core_data 依赖上层) + settings。
  - 验收：`uv sync` 成功，ruff/mypy 零错误；import-lint 拦截上层引用。
  - 自测：`pre-commit run --all-files` 全绿；写 `from app_l1 import x` 验证被拦。
- **F0.2 数据库接入与迁移**：SQLAlchemy 模型 + Alembic 迁移落地上面 DDL。
  - 验收：`alembic upgrade head` 建出全表+索引；`downgrade base` 干净；autogenerate 无差异。
  - 自测：testcontainers 起 PG，upgrade→downgrade→upgrade；断言表与索引存在。
- **F0.3 对象存储客户端**：S3 兼容 put/get/exists，key 含 sha256。
  - 验收：put 后 get 一致；exists 准确。
  - 自测：起 MinIO 容器，往返断言字节相等。
- **F0.4 队列与调度(Arq)**：Redis + Arq，入队/执行/cron。
  - 验收：入队任务被消费执行；失败重试。
  - 自测：入队 ping 任务断言副作用发生。
- **F0.5 可观测与配置基线**：结构化日志 + /healthz(DB/Redis/S3) + 统一异常重试。
  - 验收：healthz 三依赖全通 200，任一断开 503。
  - 自测：断开 Redis 断言 503 且日志含原因。

### E1 源管理 (MUST/SHOULD)
- **F1.1 源 CRUD 与模型** (MUST)：create/update/list/disable + 校验 + CLI。
  - 验收：增删改查；重复 feed_url 被拒；禁用源不参与采集。
  - 自测：建→改→禁用断言状态；重复 url 抛唯一约束；CLI 手测。
- **F1.2 OPML 批量导入** (MUST)：解析 OPML 幂等建源。
  - 验收：导入 N 条得 N 源；重复导入数量不变。
  - 自测：样例 OPML 导入两次断言源数不变。
- **F1.3 源健康监控** (SHOULD)：记录 ok/fail/empty，异常告警。
  - 验收：连续 N 次失败 status=degraded；恢复回 healthy。
  - 自测：模拟连续失败断言状态翻转与告警。

### E2 采集调度 (MUST/SHOULD)
- **F2.1 周期调度器** (MUST)：cron 扫描 enabled 源按 interval 入队。
  - 验收：到期源恰入队一次；未到期不入队；禁用跳过。
  - 自测：构造不同 last_run 断言入队集合。
- **F2.2 crawl_jobs 记录** (MUST)：每次采集开 job 记起止/stats/错误。
  - 验收：每次采集恰一条 job，stats/status 正确。
  - 自测：跑一次断言 job.stats.new == 实际新增。
- **F2.3 任务锁与去重执行** (MUST)：Redis 锁同源不并发。
  - 验收：并发触发同源只执行一个。
  - 自测：并发入队 3 个同源断言只 1 个执行。
- **F2.4 抓取礼貌** (SHOULD)：robots + 源级限速并发 + 退避。
  - 验收：robots 禁止路径不抓；超速节流；失败退避重试。
  - 自测：mock robots disallow 断言跳过；高频断言间隔≥阈值。

### E3 RSS 采集 (MUST)
- **F3.1 RSS 解析与抓取**：httpx(ETag 增量) + feedparser，容错畸形 feed。
  - 验收：正常 feed 全条目；畸形不致命；304 跳过。
  - 自测：本地样例 RSS(正常/畸形/空)断言；mock 304 断言跳过。
- **F3.2 原始 entry 保存**：原始字段全量入 raw_entry。
  - 验收：raw_entry 含源条目全部字段，可还原不丢。
  - 自测：断言 raw_entry 字段集 ⊇ feedparser 输出关键字段。
- **F3.3 增量发现**：按 url_hash 仅处理新链接。
  - 验收：重复拉同 feed 不重复创建实体。
  - 自测：连拉两次断言第二次 new=0。

### E4 网页全文抓取 (MUST)
- **F4.1 抓取器(静态+渲染)**：httpx；按 render 走 Playwright(池化)。
  - 验收：静态/JS 站都拿到完整内容；超时失败有明确错误。
  - 自测：本地起静态页与 JS 注入页断言都拿到目标文本。
- **F4.2 原始 HTML 入对象存储**：sha256 + put `raw/{source}/{sha256}.html`。
  - 验收：原文可取回且 sha256 校验通过；相同内容不重复存。
  - 自测：抓取→取回断言 sha256 一致；同内容二次命中已存 key。
- **F4.3 正文提取与置信度**：trafilatura/readability 提取 + 置信度；低置信进重试(原文保留)。
  - 验收：主流技术博客正文完整(无导航/广告)；低置信正确标记。
  - 自测：固定 10 样例断言正文含关键段不含导航词；金标覆盖率达标。
- **F4.4 失败处理与重试**：失败写 raw_documents(error) + 退避；不阻塞其他。
  - 验收：单条失败不影响同批；失败有完整留痕。
  - 自测：mock 500/超时断言重试次数与留痕。

### E5 去重与规范化 (MUST)
- **F5.1 指纹计算**：url_hash(规范化URL去utm) / content_hash / simhash。
  - 验收：同文不同 utm 得相同 url_hash；微调 simhash 邻近。
  - 自测：带/不带 utm 断言 url_hash 相同；近似文本断言海明距离小。
- **F5.2 去重判定(新/重复/变体)**：url_hash/content_hash/simhash 分级。
  - 验收：转载不重复进规范层代表，但原始抓取各自留痕；判定可解释。
  - 自测：同文 3 来源断言 1 代表 content_item + 3 条 raw_documents。
- **F5.3 ContentItem 规范化建立**：写 content_items(WAIT_FILTER) + 绑定 raw + 语言识别。
  - 验收：新内容生成恰一条 content_item(WAIT_FILTER)，可溯源；url_hash 唯一。
  - 自测：端到端"抓取→规范化"断言字段齐全且状态正确。

### E6 原始层与版本 (MUST/SHOULD)
- **F6.1 原始层不可变写入** (MUST)：所有抓取经 RawStore.append，仓储无 update/delete。
  - 验收：每次抓取产生一条不可变 raw_document；无更新/删除路径。
  - 自测：代码审查仓储无 update/delete；多次抓取累加记录。
- **F6.2 变更检测与版本** (SHOULD)：content_hash 变则新增 version。
  - 验收：内容更新产生新版本，历史可查；未变不产生新版本。
  - 自测：抓同 URL 两次(改/不改)断言版本数符合预期。
- **F6.3 血缘与可重放** (SHOULD)：lineage(content_id) + rebuild_canonical。
  - 验收：可溯源到原始抓取与源；删规范层能从原始层重建(不重抓)。
  - 自测：清空 content_items 调 rebuild 断言恢复一致。

### E7 事件与契约 (MUST/SHOULD)
- **F7.1 content.ingested 事件(outbox)** (MUST)：事务内写 outbox，relay 发布。
  - 验收：每条新内容恰发一次；崩溃重启不丢不重(至少一次+幂等键)。
  - 自测：建内容断言 outbox 有记录；relay 后标记已发；故障注入幂等不重复。
- **F7.2 内部查询 API(供 L1)** (MUST)：list_contents / get_content + 稳定契约字段。
  - 验收：按 WAIT_FILTER 分页稳定无重无漏；契约字段齐全。
  - 自测：插 N 条分页遍历断言总数=N 无重复；响应 schema 校验。
- **F7.3 重放接口** (SHOULD)：reprocess(scope, target_layer) 批量发事件。
  - 验收：指定范围内容全部发 reprocess，数量匹配。
  - 自测：对某源触发断言事件数=该源 content 数。

### E8 媒体抓取（后置 SHOULD）
- **F8.1 封面/图片** / **F8.2 播客音频** → 存对象存储，记 media_refs。（V1 再做）

---

## 5. 实施手册（开工就绪）

### 5.1 本地环境（docker-compose）
```yaml
services:
  postgres: { image: pgvector/pgvector:pg16, environment: {POSTGRES_USER: codepick, POSTGRES_PASSWORD: dev, POSTGRES_DB: codepick}, ports: ["5432:5432"], volumes: ["pgdata:/var/lib/postgresql/data"] }
  redis:    { image: redis:7, ports: ["6379:6379"] }
  minio:    { image: minio/minio, command: server /data --console-address ":9001", environment: {MINIO_ROOT_USER: minio, MINIO_ROOT_PASSWORD: minio123}, ports: ["9000:9000","9001:9001"], volumes: ["miniodata:/data"] }
volumes: { pgdata: {}, miniodata: {} }
```
Makefile 目标：`up / down / migrate / seed(导入OPML) / ingest / smoke / test / lint`。

### 5.2 仓库脚手架
```
packages/core_data/core_data/  config.py db/{models,session}.py storage/object_store.py
  sources/{repository,opml,health}.py
  ingest/{scheduler,rss,fetcher,extractor,dedup,canonical,raw_store,versioning}.py
  events/outbox.py  query/{api,contracts}.py  cli.py  scripts/smoke.py
  tests/{unit,integration,fixtures}
workers/ingestion/worker.py    db/ alembic/    deploy/{docker-compose.yml,.env.example,seed.opml}  Makefile
```

### 5.3 关键接口签名
```python
class ObjectStore(Protocol):
    def put_bytes(self, key, data, content_type) -> str: ...
    def get_bytes(self, key) -> bytes: ...
    def exists(self, key) -> bool: ...
def raw_html_key(source_id, sha256) -> str: return f"raw/{source_id}/{sha256}.html"

class RawStore:   # 只追加，无 update/delete
    def append(self, *, source_id, crawl_job_id, url, fetch_method,
               http_status=None, http_headers=None, raw_entry=None,
               raw_html=None, media=None, extra=None) -> RawDocument: ...

def fetch_feed(source) -> list[RssEntry]
class FetchResult: html: bytes; status: int; headers: dict; method: str
def fetch_page(url, render) -> FetchResult
class Extracted: title; author; text; published_at; confidence
def extract(html, url) -> Extracted
def url_hash(url)->str; def content_hash(text)->str; def simhash(text)->int
class DedupResult(Enum): NEW; SAME_URL; DUPLICATE; VARIANT
def build_content_item(raw, ex) -> ContentItem   # status=WAIT_FILTER

def emit(session, topic, payload) -> None        # 同事务写 outbox
# query 契约（见第 8 节共享契约）
def list_contents(status, since, limit, cursor) -> Page
def get_content(content_id) -> ContentOut
def reprocess(scope, target_layer) -> int
```

### 5.4 配置(.env)
```
DATABASE_URL=postgresql+psycopg://codepick:dev@localhost:5432/codepick
REDIS_URL=redis://localhost:6379/0
S3_ENDPOINT=http://localhost:9000  S3_ACCESS_KEY=minio  S3_SECRET_KEY=minio123  S3_BUCKET=codepick-raw
DEFAULT_CRAWL_INTERVAL_MIN=60  FETCH_TIMEOUT_SEC=30  PLAYWRIGHT_POOL_SIZE=2
SOURCE_MAX_CONCURRENCY=2  EXTRACT_CONFIDENCE_MIN=0.6  RESPECT_ROBOTS=true  RETRY_MAX_ATTEMPTS=3
```

### 5.5 冒烟自检（make smoke）
```
1. 导入 1 个测试源 → 2. 同步 crawl_source → 3. assert raw_documents>0
4. assert content_items(WAIT_FILTER)>0 → 5. get_content 取回 clean_text 非空 + 原文可溯源
6. assert outbox content.ingested>0 → 7. relay 后标记已发 → 8. print "L0 PIPELINE: PASS"
```

---

## 6. 测试策略
- **单元**：URL规范化/hash/simhash/OPML/正文提取，mock IO，覆盖率≥80%。
- **集成**：testcontainers 起真 PG/Redis/MinIO；迁移/仓储/队列/对象存储往返；outbox 投递幂等。
- **端到端/数据质量**：本地 RSS+HTML fixtures 跑全链；正文提取金标对比；去重场景断言。
- fixtures：`tests/fixtures/{rss,html,opml,golden}/`。CI 门禁：ruff+mypy+单元+集成，覆盖率不达标阻断。

---

## 7. 完成判定（DoD）
- 全链自动运行 ≥24h 无人工干预（OPML→RSS→全文抓取）
- 原始 entry/HTML/元数据完整留存可取回校验
- content_items 状态正确、可溯源到 raw_document
- 转载/重复正确去重，原始层不丢变体
- 内容更新产生新版本，历史可查
- content.ingested 不丢不重，L1 能稳定拉取
- 可重放：能从原始层重建规范层
- 覆盖率达标、CI 全绿、/healthz 正常、每次采集有 job 与指标

---

## 8. 共享契约（全平台一致 · 任何层不得擅改）

> 这是 4 层能拼起来的根据。L0 必须严格按此实现"对外暴露"部分。

### 事件主题（Redis 事件总线）
| 事件 | 载荷 | 发出 | 消费 |
|---|---|---|---|
| `content.ingested` | `{content_id, lang}` | **L0** | L1 |
| `content.analyzed` | `{content_id}` | L1 | L2 |
| `content.completed` | `{content_id}` | L2 | L3 |
| `content.reprocess` | `{content_id, target_layer}` | 任意 | 对应层 |

### 内容状态机（全局唯一）
`WAIT_FILTER → WAIT_ANALYSIS → WAIT_SCORE → WAIT_REVIEW → COMPLETED | CANCELLED`
前台/对外只消费 `COMPLETED`。**L0 负责把内容置为初始态 `WAIT_FILTER`**。

### 表归属（互不重叠）
- **L0**：`sources, source_health, crawl_jobs, raw_documents, content_items, content_versions, outbox_events`
- L1：`content_base_analysis, enrichment_cache`（只读 content_items）
- L2：`verticals, content_vertical_scores, content_translations, review_queue`（只读 content_items, content_base_analysis）
- L3：`users, subscriptions, user_interests, user_follows, reading_events, briefs, bookmarks, api_keys, api_usage_daily, companion_usage`（只读 L0–L2）

### L0 对上层的查询契约（冻结，L1 依赖）
```python
class ContentOut(BaseModel):
    id: int; canonical_url: str; title: str|None; clean_text: str
    lang: str|None; source: SourceRef; published_at: datetime|None; status: str
def list_contents(status, since, limit, cursor) -> Page[ContentRef]
def get_content(content_id) -> ContentOut
```

### 技术基线（全平台一致）
Python 3.12 / PostgreSQL 16 + pgvector / Redis + Arq / SQLAlchemy 2 + Alembic / S3 兼容对象存储。
开发期：fixtures + 不依赖上层；CI 用 testcontainers。

---
*CodePick L0 数据底座 · 开发文档（自包含）· 2026-05-30*
